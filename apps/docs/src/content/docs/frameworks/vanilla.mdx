---
title: Vanilla TypeScript
description: The runtime on its own, with no framework anywhere in the dependency graph.
integration:
  kind: framework
  context: vanilla
  concept: overview
---

import BrowserQuerySimulator from "../../../components/BrowserQuerySimulator.astro";
import IntegrationHeader from "../../../components/IntegrationHeader.astro";
import IntegrationNav from "../../../components/IntegrationNav.astro";

<IntegrationHeader context="vanilla" />

This is not a fallback. It is the baseline every other integration is built on: a model, an adapter,
a runtime, and a render function you write yourself.

## Install

```sh
pnpm add @queryweave/core @queryweave/browser
```

## Example

```ts title="filters.ts" {10-14,34-36}
import { createBrowserAdapter } from "@queryweave/browser";
import { createQueryRuntime, defineQueryModel, param } from "@queryweave/core";

const products = defineQueryModel({
  search: param.text().optional(),
  page: param.integer({ min: 1 }).default(1),
  status: param.choice(["all", "active", "archived"]).default("all"),
});

const runtime = createQueryRuntime({
  model: products,
  adapter: createBrowserAdapter(),
  throttle: 300,
});

const searchInput = document.querySelector<HTMLInputElement>("#search");
const results = document.querySelector<HTMLElement>("#results");

function render(values: ReturnType<typeof runtime.read>["values"]): void {
  if (searchInput !== null && searchInput.value !== (values.search ?? "")) {
    searchInput.value = values.search ?? "";
  }
  if (results !== null) {
    results.textContent = `Page ${String(values.page)} · ${values.status}`;
  }
}

runtime.subscribe((snapshot) => {
  render(snapshot.values);
});

render(runtime.read().values);

searchInput?.addEventListener("input", () => {
  void runtime.update({ search: searchInput.value || undefined }, { navigation: "replace" });
});
```

Four details are worth naming:

- **Render on subscribe _and_ once immediately.** Subscribing does not deliver the current state;
  it delivers changes. Read once to paint the first frame.
- **Guard the input assignment.** Writing the same value back into a focused field moves the
  caret. Compare first.
- **`|| undefined` clears the key.** An empty string would produce `?search=` and an `empty` issue;
  `undefined` omits it. See [defaults and absence](/concepts/defaults-and-absence/).
- **`throttle` replaces a debounce.** The first keystroke is written at once and the rest of a
  burst together when the window closes, so the input handler stays a plain `update`. See
  [Transitions](/runtime/transitions/#throttling).

## Reacting to the back button

Nothing extra is needed. The adapter listens for `popstate`, and your subscriber receives the same
snapshot it would after a programmatic transition.

<BrowserQuerySimulator
  preset="filters"
  adapter="browser"
  caption="No framework is involved here either — the simulator on this page is plain TypeScript over the same runtime."
/>

## Throttling

The example above gives the runtime a 300 ms window, so a search field produces two writes per
burst rather than one per keystroke, and `navigation: "replace"` keeps those out of the history
stack. Without a window the runtime applies every transition immediately. See
[Transitions](/runtime/transitions/#throttling) for the rules, and for abandoning a transition
through an `AbortSignal`.

## Cleanup

```ts {1,4-5}
const unsubscribe = runtime.subscribe(handler);

// later
unsubscribe();
runtime.dispose();
```

In a page-lifetime application you can skip this. In anything that mounts and unmounts, do not.

## Server rendering without a framework

The same model reads the request:

```ts {3-5}
import { readRequestQuery } from "@queryweave/server";

const result = readRequestQuery(request, products);
const values = result.ok ? result.value : { ...products.defaults(), ...result.partial };
// Render the same search, page, and filters the client will read from this URL.
```

The client then creates its runtime and immediately reads the same URL, so the first client render
matches what the server produced — no state transfer required, because the URL _is_ the transfer.

<IntegrationNav />
